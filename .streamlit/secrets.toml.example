# Streamlit Secrets 템플릿 (실제 키를 이 파일에 넣지 말 것 → .gitignore에 포함됨)
# 배포: 앱의 ⋮ → Settings → Secrets 에 이 내용을 붙여넣어 저장하세요.
MFDS_API_KEY = "여기에 MFDS(의약품 제품 허가정보) 데이터활용신청 API 인증키"
HIRA_API_KEY = "여기에 HIRA(약가정보) 데이터활용신청 API 인증키"

# 두 서비스를 같은 data.go.kr 키로 신청했다면 아래 하나로 통합 사용 가능:
# DATA_GO_KOR_API_KEY = "여기에 data.go.kr API 인증키"
